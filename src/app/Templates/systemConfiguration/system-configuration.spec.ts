import { SystemConfiguration } from './system-configuration';

describe('SystemConfiguration', () => {
  it('should create an instance', () => {
    expect(new SystemConfiguration()).toBeTruthy();
  });
});
